#include <stdio.h>
#include <math.h>
int main()
{
    int i,j,n=3,k=20,a;
    float b;
    for (i=0;i<=1;i++)
    {
        for (j=0;j<=k;j++)
        {
            a=pow(2,j);
            b=pow(2,-j);
            printf("%d      %d      %f\n",a,j,b );
        }
    }


}
