// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>
int main()
{
    int n,f,l,rem;
    printf("enter number:");
    scanf("%d",&n);
    l=n%10;
    while(n!=0)
        {
            f=n%10;
            n=n/10;  
        }
    printf("first:%d last:%d",f,l);

}
