// Online C compiler to run C program online
#include <stdio.h>
int main()
{
    int i,m[50],value,j,count,a,b,c,count10=0;
    for(i=0;i<50;i++)
    {
        printf("enter mark:");
        scanf("%d",&value);
        m[i]=value;
    }   
    printf("group       range       fre\n");
    for(i=0;i<10;i++)
    {
        count=0;
        count10=0;
        for(j=0;j<50;j++)
            {
                a=m[j]/10;
                if(a==i)
                count=count+1;
                else if(a==10)
                count10=count10+1;

            }
        b=(i)*10;
        c=b+9;
        printf("%d       %d to %d      %d\n",i,b,c,count);

    }
        printf("11       100 to 100    %d\n",count10);

    
}
