#include <stdio.h>
#include <math.h>

int main()
{
     int a,b,n,i,gdc;
    printf("enter a and b;");
    scanf("%d %d",&a,&b);
    if(a>b)
    n=a;
    else if(b>a)
    n=b;
    for(i=1;i<=n;i++)
        {
            if(a%i==0 && b%i==0)
            gdc=i;
        }
    printf("GDC:%d",gdc);
}
