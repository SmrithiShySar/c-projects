#include <stdio.h>

int main()
{
    int i,sum,count;
    count=0;
    sum=0;
    for(i=1;i<=100;i++)
    {
        if (i%6==0 && i%4!=0)
        {
            sum=sum+i;
            count=count+1;
        }
    }
    printf("sum and count:%d %d",sum,count);
}
