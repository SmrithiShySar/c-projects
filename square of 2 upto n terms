#include <stdio.h>
int main()
{
    int n,a,i;
    printf("enter the number:");
    scanf("%d",&n);
    a=1;
    for(i=1;i<=n;i++)
        {
            a*=2;
            printf("%d\n",a);
        }
}
