#include <stdio.h>
int main()
{
    int count,sum,i;
    count=0;
    sum=0;
    for (i=101;i<=200;i++)
    {
        if (i%7==0)
        {
            count=count+1;
            sum=sum+i;
        }
    }
    printf("Count:%d\n",count);
    printf("Sum:%d\n",sum);
}
