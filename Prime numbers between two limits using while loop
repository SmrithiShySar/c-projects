#include <stdio.h>
int main()
{
    int i,prime,a,b,num,count,c;
    printf("enter the limits");
    scanf("%d %d",&a,&b);
    num=a;
    while(num>=a && num<=b)
    {
        i=1;
        count=0;
        while(i<=num)
        {
            if(num%i==0)
            count=count+1;
            i=i+1;
        }
        if(count==2)
        printf("%d\n",num);
        num=num+1;
    }
}
