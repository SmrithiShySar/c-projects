#include <stdio.h>
#include <math.h>

int main()
{
    int n=456,sum=0,rem;
    while(n!=0)
    {
        rem=n%10;
        sum=sum+rem;
        n=n/10;
        if(n==0 && sum>9)
        {
            n=sum;
            sum=0;
        }
    }
    printf("generic root:%d",sum);
}
